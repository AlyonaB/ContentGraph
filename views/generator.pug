extends layout

block content
    form(action='/generate', method='POST')
        div
            input(
                type='text',
                name='url'
            )
        input(
            class='ui-button ui-widget ui-corner-all',
            type='submit',
            value='Generate'
        )